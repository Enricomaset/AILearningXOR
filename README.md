# AILearningXOR
A from-scratch exploration of how a 2–2–1 neural network learns the XOR function. The project explains the underlying mathematics—from nonlinear space transformation to loss minimization and backpropagation—and implements the complete training process in C++ without machine-learning libraries.
